description = "Ask ChatGPT for a plan, then execute it."
prompt = """
You are the EXECUTOR agent.

1) Read the PLAN below (generated by ChatGPT).
2) Follow it exactly with minimal diffs.
3) Run the suggested tests/commands.
4) Report back with: what changed, diff summary, commands ran, current status.

PLAN (from ChatGPT):
!{bash -lc 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"; ./scripts/gpt_plan.sh'}
"""
